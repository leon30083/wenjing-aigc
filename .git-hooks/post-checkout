commit e5e6c55b3d4aee9ffa8fd266ff98ded33644758a
Author: 罗森 <leon3000@live.cn>
Date:   Fri Jan 23 10:58:53 2026 +0800

    fix: 修复 Git Hooks 编码问题
    
    - 移除 emoji 字符，使用纯文本输出
    - 修复 PowerShell 解析错误
    - 更新 Bash 和 PowerShell hook 脚本
    - 测试验证 hooks 正常触发

diff --git a/.git-hooks/post-checkout b/.git-hooks/post-checkout
index d85f31e..1caf7c8 100644
--- a/.git-hooks/post-checkout
+++ b/.git-hooks/post-checkout
@@ -1,26 +1,26 @@
 #!/bin/bash
 # Git post-checkout hook
 #
-# 触发时机: git checkout 或 git branch 切换后
-# 功能: 检测配置变化，提示用户同步配置
+# Trigger: git checkout or git branch switch
+# Function: Check config changes and prompt user to sync
 #
-# 参数: $1 = previous ref, $2 = new ref, $3 = branch checkout flag (1=branch)
+# Args: $1 = previous ref, $2 = new ref, $3 = branch checkout flag (1=branch)
 
-# 只在分支切换时检查（不适用于文件签出）
+# Only check on branch switch (not file checkout)
 if [ "$3" != "1" ]; then
   exit 0
 fi
 
 echo ""
-echo "🔍 [Git Hook] 检测到分支切换，正在检查配置..."
+echo "[Git Hook] Detected branch switch, checking config..."
 echo ""
 
-# 运行配置检查脚本（Git hook 模式）
+# Run config check script (Git hook mode)
 node scripts/check-claude-sync.js --git-hook
 
-# 检查退出码
+# Check exit code
 if [ $? -ne 0 ]; then
   echo ""
-  echo "💡 提示: 运行 'npm install' 同步最新配置"
+  echo "[Git Hook] Config needs sync. Run: npm install"
   echo ""
 fi
