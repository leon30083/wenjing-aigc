# WinJin AIGC Electron 打包修复说明

> **更新日期**: 2026-01-29
> **版本**: 2.0.1 (Hotfix)

---

## 🔧 本次修复内容

### 修复的问题

1. **后端服务启动验证** ⭐ 核心修复
   - 添加服务器启动等待和验证逻辑
   - 通过 HTTP 请求验证后端是否真正启动
   - 最多等待 10 秒，超时则显示错误对话框

2. **路径修复**
   - 正确识别打包后的服务器文件路径
   - 使用 `app.asar.unpacked` 路径访问服务器代码

3. **环境变量文件包含**
   - `.env` 文件现在会被正确解包到 `app.asar.unpacked/`

4. **数据目录包含**
   - `data/` 目录及其内容（characters.json, history.json）现在会被正确解包

5. **错误处理改进**
   - 捕获并显示后端服务器的输出
   - 通过对话框显示错误信息给用户
   - 添加详细的日志输出

---

## 🚀 使用新版本

### 快速测试

1. **关闭旧版本**（如果正在运行）
2. **运行新版本**
   ```
   双击: WinJin AIGC.exe
   ```

### 预期行为

**正常启动流程**（约 3-5 秒）：
```
[Main] 打包模式 - 服务器路径: ...
[Main] 打包模式 - 工作目录: ...
[Main] 正在启动 Express 服务器...
[Main] 等待服务器启动...
[Server] Express 服务器运行在端口 9000
[Main] ✅ 服务器启动成功！
[应用窗口打开]
```

**如果出现问题**：
- 会弹出错误对话框显示具体问题
- 开发者工具中会显示详细日志

---

## 🐛 问题排查

### 如果仍然无法连接后端

**步骤 1**: 打开开发者工具
```
方法 1: 按 Ctrl + Shift + I
方法 2: 右键点击窗口 → 检查元素
```

**步骤 2**: 查看 Console 标签页
- 查找 `[Main]` 和 `[Server]` 开头的日志
- 截图所有错误信息

**步骤 3**: 查看 Network 标签页
- 尝试执行任何操作（如配置 API）
- 查看失败的请求（标红）
- 截图请求详情

**步骤 4**: 手动验证后端
```
在浏览器访问: http://localhost:9000

如果看到 "Cannot GET /" → 后端正在运行 ✅
如果看到 "无法访问" → 后端未启动 ❌
```

### 常见错误及解决方案

#### 错误 1: "ECONNREFUSED"
```
错误信息: connect ECONNREFUSED 127.0.0.1:9000
```

**原因**: 后端服务未启动或端口被占用

**解决方案**:
```bash
# 1. 检查端口占用
netstat -ano | findstr :9000

# 2. 如果被占用，终止进程
taskkill /F /PID <PID>

# 3. 重新启动应用
```

#### 错误 2: "SORA2_API_KEY is not defined"
```
错误信息: SORA2_API_KEY is not defined
```

**原因**: .env 文件缺失或配置错误

**解决方案**:
检查 `release/win-unpacked/resources/app.asar.unpacked/.env` 文件是否存在

**内容应该包含**:
```bash
SORA2_API_KEY=sk-xxxxx
ZHENZHEN_API_KEY=sk-xxxxx
PORT=9000
```

#### 错误 3: "Cannot find module"
```
错误信息: Error: Cannot find module 'xxx'
```

**原因**: 依赖缺失（打包时可能遗漏）

**解决方案**:
这是一个打包问题，需要重新构建

---

## 📊 新增的日志输出

### 主进程日志

```
[Main] 打包模式 - 服务器路径: ...
[Main] 打包模式 - 工作目录: ...
[Main] 正在启动 Express 服务器...
[Main] 等待服务器启动...
[Main] 等待服务器... (1.0s)
[Main] 等待服务器... (1.5s)
[Main] ✅ 服务器启动成功！
```

### 服务器日志

```
[Server] Express 服务器运行在端口 9000
```

### 错误日志

```
[Server Error] ... (任何服务器输出的错误)
[Main] ❌ 服务器启动超时
```

---

## ✅ 验证清单

运行新版本后，请检查：

- [ ] 应用窗口正常打开
- [ ] 前端界面正常显示
- [ ] 控制台显示 "[Main] ✅ 服务器启动成功！"
- [ ] 可以访问 http://localhost:9000
- [ ] API 配置功能正常
- [ ] 角色库功能正常
- [ ] 视频生成功能正常

---

## 🆘 如果问题仍未解决

请提供以下信息：

1. **开发者工具 Console 日志**（截图）
2. **开发者工具 Network 日志**（截图，失败的请求）
3. **错误对话框内容**（如果弹出）
4. **操作系统版本**（Windows 10/11）

---

**维护者**: WinJin AIGC Team
**版本**: 2.0.1 (Hotfix)
**最后更新**: 2026-01-29
