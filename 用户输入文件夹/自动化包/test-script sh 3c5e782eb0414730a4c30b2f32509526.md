# test-script.sh

```bash
#!/bin/bash

echo "🚀 启动提示词测试..."

# 创建测试结果目录
mkdir -p test-results

# 遍历所有版本
for version in "$@"; do
  echo "\n📝 测试版本: $version"
  
  # 调用测试API
  bun run test:prompt --version=$version
  
  # 记录结果
  echo "✅ $version 测试完成"
done

# 生成对比报告
bun run generate:report

echo "\n🎉 所有测试完成！查看 test-results/ 目录"
```

## 文件位置

`wenjing-aigc/.claude/skills/prompt-tester/[test-script.sh](http://test-script.sh)`

## 使用说明

1. 复制上述脚本到文件
2. 添加执行权限：`chmod +x [test-script.sh](http://test-script.sh)`
3. [配合instructions.md](http://配合instructions.md)使用，Claude会自动调用该脚本

## 前置条件

需要在项目的`package.json`中配置：

```json
{
  "scripts": {
    "test:prompt": "node scripts/test-prompt.js",
    "generate:report": "node scripts/generate-report.js"
  }
}
```