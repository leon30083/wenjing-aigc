# 📖 使用指南

# Claude Code 自动化配置使用指南

## 🎯 配置目标

让Claude Code自动：

- 记住项目规范和技术栈
- 执行常用命令无需频繁授权
- 在编辑特定文件时应用专属规则
- 一键调用测试工作流

---

## 📂 文件部署清单

### 步骤1：创建目录结构

```bash
cd ~/wenjing-aigc

# 创建.claude目录
mkdir -p .claude/rules
mkdir -p .claude/skills/prompt-tester/examples
```

### 步骤2：复制配置文件

从本页面的子页面依次导出并复制：

| 子页面 | 目标路径 |
| --- | --- |
| [CLAUDE.md](http://CLAUDE.md) | `wenjing-aigc/[CLAUDE.md](http://CLAUDE.md)` |
| settings.json | `wenjing-aigc/.claude/settings.json` |
| [prompt-optimizer.md](http://prompt-optimizer.md) | `wenjing-aigc/.claude/rules/[prompt-optimizer.md](http://prompt-optimizer.md)` |
| prompt-tester [instructions.md](http://instructions.md) | `wenjing-aigc/.claude/skills/prompt-tester/[instructions.md](http://instructions.md)` |
| [test-script.sh](http://test-script.sh) | `wenjing-aigc/.claude/skills/prompt-tester/[test-script.sh](http://test-script.sh)` |

### 步骤3：设置脚本权限

```bash
chmod +x .claude/skills/prompt-tester/[test-script.sh](http://test-script.sh)
```

---

## ✅ 验证配置

### 1. 启动Claude Code

```bash
cd ~/wenjing-aigc
claude
```

### 2. 运行验证命令

```bash
# [验证CLAUDE.md](http://验证CLAUDE.md)加载
/context
# 👀 查看"内存文件"部分，[应该能看到CLAUDE.md](http://应该能看到CLAUDE.md)

# 验证Sandbox权限
/sandbox
# 👀 应该显示允许的命令列表

# 验证Hooks
/hooks
# 👀 查看PreToolUse和PostToolUse配置

# 验证Skills
/skills
# 👀 应该看到prompt-tester技能
```

---

## 🚀 实战场景

### 场景1：开发新功能（Plan Mode）

```bash
# 进入计划模式
Shift+Tab (按两次)

# 描述需求
> 添加提示词版本对比功能

# Claude会先分析架构，等你确认后再动手
```

### 场景2：调试Bug（Ultrathink）

```bash
# 深度思考模式
> ultrathink: 为什么提示词优化后Token反而增加了？

# 立即执行相关命令
! bun run analyze:tokens
```

### 场景3：提示词A/B测试

```bash
# 调用测试技能
/skills prompt-tester --versions v2.0,v2.1

# 自动并行测试，生成对比报告
```

### 场景4：修改提示词文件

编辑`src/features/prompt-optimizer/prompts.ts`时，Claude会自动：

1. 检查Token计数
2. 验证格式规范
3. 提示生成测试用例
4. 建议版本号

---

## 💡 高级技巧

### 自定义命令

在`.claude/`目录下创建markdown文件即可变成斜杠命令：

```bash
# 创建每日站会命令
echo "生成今日开发总结和明日计划" > .claude/[daily-standup.md](http://daily-standup.md)

# 使用
/daily-standup
```

### 添加新的Rules

```bash
# 为API开发添加规则
touch .claude/rules/[api-dev.md](http://api-dev.md)
```

在文件头部指定路径匹配：

```markdown
---
paths: src/api/**/*.ts
---
# API开发规则
```

---

## ⚠️ 常见问题

### Q: [CLAUDE.md](http://CLAUDE.md)不生效？

**解决**：

```bash
# 确认文件在根目录
ls -la [CLAUDE.md](http://CLAUDE.md)

# 重启会话
claude --continue
```

### Q: Rules没有触发？

**检查**：

1. 文件头部是否有`---\npaths: ...\n---`
2. 路径匹配是否正确
3. 文件名是否为`.md`格式

### Q: Skills找不到？

**检查**：

```bash
# 确认目录结构
ls -la .claude/skills/prompt-tester/

# [应该包含instructions.md](http://应该包含instructions.md)
```

### Q: Sandbox权限不生效？

**解决**：

```bash
# 检查JSON格式
cat .claude/settings.json | jq .

# 重新加载配置
/config reload
```

---

## 🔄 配置更新流程

修改任何配置文件后：

```bash
# 方式1：重启会话
claude --continue

# 方式2：手动重载
/config reload

# 验证生效
/context  # 查看新配置
```

---

## 📊 效果对比

| 操作 | 配置前 | 配置后 |
| --- | --- | --- |
| 执行`npm install` | 每次询问 | 自动执行 |
| 说明项目背景 | 每次重复 | 自动加载 |
| 提示词测试 | 手动逐个 | 一键并行 |
| 编辑提示词文件 | 无检查 | 自动规范验证 |

---

## 🎁 下一步

1. **扩展Skills**：根据实际需求添加更多工作流
2. **优化Rules**：细化不同模块的开发规则
3. **集成CI/CD**：通过Hooks对接部署流水线
4. **团队共享**：通过Plugins打包分享配置

---

## 📚 参考资源

- [用了这31招Claude Code直接起飞（下）](https://www.notion.so/31-Claude-Code-80feb2bc254c4b5b9fe1dbd204d7a73d?pvs=21)
- [用了这31招Claude Code直接起飞（中）](https://www.notion.so/31-Claude-Code-bc82a811ef124fb69e9c73c1d7134874?pvs=21)
- [用了这31招Claude Code直接起飞（上）](https://www.notion.so/31-Claude-Code-7c734357a2b14963a91911ee31c4e457?pvs=21)